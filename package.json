{
  "name": "leave-management-app",
  "version": "1.0.0",
  "description": "Leave management app with a secure admin approval panel.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "echo 'No build step required'",
    "vercel-build": "echo 'No build step required'",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "pg": "^8.13.1",
    "nodemailer": "^6.9.13"
  }
}
